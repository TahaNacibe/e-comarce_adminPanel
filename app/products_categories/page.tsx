'use client';

import styles from './page.module.css';

export default function AdminPanel() {
    return (
        <div style={{
            display: 'flex',
            height: '100vh',
            justifyContent: 'center',
            alignItems: 'center',
        }}>
            <h1>Admin Panel</h1>
        </div>
    );
}