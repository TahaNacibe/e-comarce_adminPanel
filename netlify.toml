[[headers]]
  for = "/api/orders"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
    Pragma = "no-cache"
    Expires = "0"
[build]
  functions = "netlify-functions"

[functions]
  max_duration = 60  # Max function duration in seconds
